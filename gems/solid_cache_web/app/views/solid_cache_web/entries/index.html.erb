<% content_for :title, "Cache entries" %>
<%= render SolidWebUi::Ui::PageComponent.new(title: "Cache entries", nav: cache_nav(:entries)) do %>
  <%= render SolidWebUi::Ui::TableComponent.new(headers: [ "ID", "Key", "Size", "Created" ], empty_message: "The cache is empty.") do %>
    <% @entries.each do |entry| %>
      <tr>
        <td><%= entry.id %></td>
        <td><code><%= readable_key(entry.key) %></code></td>
        <td><%= number_to_human_size(entry.byte_size) %></td>
        <td><%= short_time(entry.created_at) %></td>
      </tr>
    <% end %>
  <% end %>

  <%= render SolidWebUi::Ui::PaginatorComponent.new(paginator: @paginator, page_url: ->(page) { entries_path(page: page) }) %>
<% end %>
